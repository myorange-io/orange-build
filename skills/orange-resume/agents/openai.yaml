interface:
  display_name: "Orange Resume"
  short_description: "현재 IA 단계와 승인 상태에서 작업을 이어갑니다."
  default_prompt: "Use $orange-resume to inspect the current IA step, approval state, completion level, and evidence, then continue from that exact point."
